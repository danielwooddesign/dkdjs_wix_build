{
  "jobs": [
    {
      "functionLocation": "/availability.web.js",
      "functionName": "expireStaleHolds",
      "description": "Release dates whose 7-day hold lapsed without confirmation",
      "executionConfig": {
        "cronExpression": "0 * * * *"
      }
    }
  ]
}
